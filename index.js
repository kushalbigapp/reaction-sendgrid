/* import registerSendgridPlugin from "./sendgridPlugin";

const api = new ReactionAPI();

async function startAPI() {
  await registerSendgridPlugin(api);
  await api.start();
}

startAPI().catch((error) => {
  console.error(error);
}); */
